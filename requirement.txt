# Core runtime deps
mediapipe==0.10.14
protobuf==4.25.8

# CV / math / plotting
opencv-contrib-python==4.12.0.88
numpy==2.2.6
matplotlib==3.10.8
pillow==11.3.0

# Optional (only if the scripts that use it are run)
scipy==1.15.3
sounddevice==0.5.3